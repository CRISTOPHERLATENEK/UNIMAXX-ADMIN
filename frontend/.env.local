# Arquivo de desenvolvimento local — NÃO é usado no build de produção
# (o Vite usa .env.production durante "npm run build")
# Descomente a linha abaixo apenas para desenvolvimento local:
# VITE_API_URL=http://localhost:3001/api
